import { colors } from '@/providers/theme';
import { Icon } from '.';

const BinIcon = () => {
	return <Icon sx={{ color: colors.red }} fontSize='large' name={'delete'} />;
};

export default BinIcon;
